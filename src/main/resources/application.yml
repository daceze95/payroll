server:
  port: 8080 #specify your port number
spring:
  dataSource:
    #    url: jdbc:postgresql://localhost:{db portNumber}/{databaseName}
    url: jdbc:postgresql://localhost:5436/mac
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update #update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
